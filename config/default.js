const config = {
    port: 8000,

    database: {
        DATABASE: 'homework_box',
        USERNAME: 'root',
        PASSWORD: 'zyy1001happiness',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;
